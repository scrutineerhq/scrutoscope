{
  "name": "scrutinizer-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Browser QA tooling for the Scrutinizer dashboard (dev-only; excluded from the wp.org build via .distignore).",
  "scripts": {
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.49.1"
  }
}
